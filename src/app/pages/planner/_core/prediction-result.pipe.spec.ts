import { PredictionResultPipe } from './prediction-result.pipe';

describe('PredictionResultPipe', () => {
  it('create an instance', () => {
    const pipe = new PredictionResultPipe();
    expect(pipe).toBeTruthy();
  });
});
